import { IconType } from "react-icons"

export interface SortOption {
   id: string
   name: string
   icon: IconType
}

export const SortListBox = () => {
   return (
      <div>SortListBox</div>
   )
}